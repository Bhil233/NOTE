# 启动
```
uvicorn main:app --reload
```
main（主程序文件名）
app（实例名）
{--reload}（热加载）

访问FastAPI交互式文档
http://127.0.0.1:8000/does

# 路由

```
@app【FastAPI实例】.get【请求方法】("/")【请求路径】
async def root():
	return {"msg": "hello world"}
```

# 路径参数
**位置**：URL路径的一部分
**作用**：指向唯一的、特定的资源
**方法**：GET